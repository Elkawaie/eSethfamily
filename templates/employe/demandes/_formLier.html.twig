{{ form_start(form) }}
<div class="form-group">
    {{ form_label(form.famille) }}
    {{ form_widget(form.famille, {'attr':{'class': 'form-control'}}) }}
</div>
<div class="form-group">
    {{ form_label(form.resident) }}
    {{ form_widget(form.resident, {'attr':{'class': 'form-control'}}) }}
</div>
{{ form_widget(form) }}
<button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
