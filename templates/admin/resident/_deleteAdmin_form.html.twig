<form class="mb-5 ml-3 mt-5" method="post" action="{{ path('resident_delete', {'id': resident.id}) }}" onsubmit="return confirm('Etes-vous sur de bien vouloir supprimer ce résident ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resident.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
